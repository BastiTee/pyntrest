#!/bin/bash

HOST=localhost
PORT=8000
USER=$( whoami )

scriptdir="$(dirname "$0")"
echo "cd'ing to $scriptdir .."

cd ${scriptdir}
sudo -u $USER python manage.py runserver ${HOST}:${PORT} $@ >> stdout.log 2>> stderr.log &
tail -f -n100 stdout.log stderr.log

